# Optional. The MVP does not require a real key to run.
MINIMAX_API_KEY=
MINIMAX_MODEL=MiniMax-M3
MINIMAX_BASE_URL=https://api.minimax.io

# Future real AWS provider settings. The mock provider ignores these.
AWS_ROLE_ARN=
AWS_REGION=us-east-1
